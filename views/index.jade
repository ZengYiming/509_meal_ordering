首页

if(typeof(dishes) != 'undefined')
  table
    tr
        th
            菜品名
        th
            价格

    - each dish in dishes
        tr
            th
                a(href="http://localhost:8888/dish_info?id=#{dish.id}") #{dish.name}
            th
                = dish.price